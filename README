A clean, minimal full-stack starter combining FastAPI (Python) and React (TypeScript).
It’s ready to run in Docker, supports live reload, and provides a well-structured base for small to medium-size applications.


| **Backend** | FastAPI + Uvicorn | Auto-reload, versioned routes (`/api/v1`) |
| **Frontend** | React + TypeScript + Vite | Fast HMR dev server |
| **DevOps** | Docker + Compose | One command to start everything |
| **Cross-origin** | CORS ready | Default `http://localhost:3000` |
| **Docs** | Swagger / Redoc | Opens automatically at `/docs` |

